/**
* The interface PortCard defines the fields present in each portait card
* component
* 
* @author Souleiman Ayoub
* @version 1.0
**/

export interface PortCard {
    img_src: string;
    title: string;
    sub_title: string;
}